HOST=127.0.0.1
PORT=3000
DATABASE_PATH=/var/lib/monkeytype-duel/duel.sqlite
ALLOWED_ORIGIN=https://keeb.makerspace.tools
RACE_DURATION_SECONDS=30
# Required for /admin.html. Use a long random value and enter it only in the
# operator dashboard; it is never bundled into the Pages client.
ADMIN_TOKEN=
# Optional. Raises GitHub API limits for uncached public-profile lookups.
# GITHUB_TOKEN=
